---
it:
  errors:
    format: "%{attribute} %{message}"
    messages:
      accepted: deve essere accettata
      after: deve essere successivo a %{date}
      after_or_equal_to: deve essere successivo o uguale a %{date}
      allowed_file_content_types: il file deve essere uno %{types}
      aspect_ratio_is_not: deve avere un rapporto di %{aspect_ratio}
      aspect_ratio_not_landscape: deve essere un'immagine orizzontale
      aspect_ratio_not_portrait: deve essere un'immagine verticale
      aspect_ratio_not_square: deve essere un'immagine quadrata
      aspect_ratio_unknown: rapporto di dimensione sconosciuto
      attached: deve essere fornito
      before: deve essere precedente a %{date}
      before_or_equal_to: deve essere precedente o uguale a %{date}
      blank: campo obbligatorio
      confirmation: non coincide con %{attribute}
      content_type_invalid: non corrisponde al formato richiesto
      dimension_height_equal_to: altezza deve essere uguale a %{length} pixel.
      dimension_height_greater_than_or_equal_to: altezza deve essere maggiore o uguale a %{length} pixel.
      dimension_height_inclusion: altezza non inclusa tra %{min} e %{max} pixel.
      dimension_height_less_than_or_equal_to: altezza deve essere minore o uguale a %{length} pixel.
      dimension_max_inclusion: deve essere minore o uguale di %{width} x %{height} pixel.
      dimension_min_inclusion: deve essere maggiore o uguale di %{width} x %{height} pixel.
      dimension_width_equal_to: larghezza deve essere uguale a %{length} pixel.
      dimension_width_greater_than_or_equal_to: larghezza deve essere maggiore o uguale a %{length} pixel.
      dimension_width_inclusion: larghezza non inclusa tra %{min} e %{max} pixel.
      dimension_width_less_than_or_equal_to: larghezza deve essere minore o uguale a %{length} pixel.
      email_is_invalid: Email non valida
      empty: non può essere vuoto
      equal_to: deve essere uguale a %{count}
      even: deve essere pari
      excluded_file_content_types: il file non può essere %{types}
      exclusion: è riservato
      file_size_is_greater_than: la dimensione del file deve essere maggiore a %{count}
      file_size_is_greater_than_or_equal_to: la dimensione del file deve essere maggiore o uguale a %{count}
      file_size_is_in: la dimensione del file deve essere tra %{min} e %{max}
      file_size_is_less_than: la dimensione del file deve essere inferiore a %{count}
      file_size_is_less_than_or_equal_to: la dimensione del file deve essere minore o uguale a %{count}
      file_size_out_of_range: la dimensione %{file_size} non è nel range richiesto
      greater_than: deve essere maggiore di %{count}
      greater_than_or_equal_to: deve essere maggiore o uguale a %{count}
      image_metadata_missing: non è una imagine valida
      inclusion: non è compreso tra le opzioni disponibili
      invalid: non è valido
      less_than: deve essere minore di %{count}
      less_than_or_equal_to: deve essere minore o uguale a %{count}
      limit_out_of_range: numero totale fuori range
      not_a_date: non è una data
      not_a_number: non è un numero
      not_an_integer: non è un numero intero
      odd: deve essere dispari
      other_than: devono essere di numero diverso da %{count}
      overlap: Si sovrappone con un altro elemento
      present: deve essere lasciato in bianco
      required: campo obbligatorio
      taken: è già presente
      too_long:
        one: è troppo lungo (il massimo è 1 carattere)
        other: è troppo lungo (il massimo è %{count} caratteri)
      too_short:
        one: è troppo corto (il minimo è 1 carattere)
        other: è troppo corto (il minimo è %{count} caratteri)
      wrong_length:
        one: è della lunghezza sbagliata (deve essere di 1 carattere)
        other: è della lunghezza sbagliata (deve essere di %{count} caratteri)
    template:
      body: 'Ricontrolla i seguenti campi:'
      header:
        one: 'Non posso salvare questo %{model}: 1 errore'
        other: 'Non posso salvare questo %{model}: %{count} errori.'

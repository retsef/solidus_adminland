<% content_for(:title) { t("administrate.actions.edit_resource", name: page.page_title) } %>

<header class="page-header d-print-none" role="banner">
  <div class="row g-3 align-items-center">
    <div class="col">
      <!-- Pretitle -->
      <div class="page-pretitle">
        <%= link_to t("administrate.actions.back"), :back %>
      </div>
      <!-- Title -->
      <h2 class="page-title text-truncate">
        <%= content_for(:title) %>
      </h2>
    </div>

    <div class="col-auto">
      <!-- Actions -->
      <%= render("edit_header_actions", page: page) %>
    </div>
  </div>
</header>
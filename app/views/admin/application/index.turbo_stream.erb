<%= render 'flashes' %>

<turbo-stream action="replace" target="collection_table">
  <template>
    <%= render(
      "collection",
      collection_presenter: page,
      collection_field_name: resource_name,
      page: page,
      resources: resources,
      table_title: "page-title"
    ) %>
  </template>
</turbo-stream>
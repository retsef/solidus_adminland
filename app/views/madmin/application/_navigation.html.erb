<nav class="navbar navbar-vertical fixed-start navbar-expand-md navbar-dark" id="sidebar">
  <div class="container-fluid">

    <!-- Toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarCollapse" aria-controls="sidebarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Brand -->
    <a class="navbar-brand" href="#">
      <img src="<%= image_path("madmin/solidus-symbol-light.png") %>" class="navbar-brand-img mx-auto" alt="Solidus">
    </a>

    <!-- User (xs) -->
    <div class="navbar-user d-md-none">

    </div>

    <!-- Collapse -->
    <div class="collapse navbar-collapse" id="sidebarCollapse">

      <%# Madmin.resources.each do |resource| %>
        <%#= nav_link_to resource.friendly_name.pluralize, resource.index_path, class: "block p-2 rounded hover:bg-gray-100", starts_with: resource.index_path, active_class: "font-bold text-black" %>
      <%# end %>

      <% Spree::Backend::Config.menu_items.sort_by { |item| item.position || Float::INFINITY }.each_with_index do |menu_item, index| %>
        <%=
          tab(
            *menu_item.sections,
            icon: menu_item.icon,
            label: menu_item.label,
            url: menu_item.url.is_a?(Symbol) ? spree.public_send(menu_item.url) : menu_item.url,
            match_path: menu_item.match_path,
          )
        %>

        <!-- Navigation -->
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="#sidebar_<%= index %>" data-bs-toggle="collapse" role="button" aria-expanded="true" aria-controls="sidebar_<%= index %>">
              <i class="fe fe-<%= menu_item.icon %>"></i> <%= menu_item.label %>
            </a>
            <div class="collapse show" id="sidebar_<%= index %>">
              <ul class="nav nav-sm flex-column">
                <%= menu_item.sections.each do |section| %>
                  <li class="nav-item">
                    <a href="#" class="nav-link active">
                      Default
                    </a>
                  </li>
                <% end %>
              </ul>
            </div>
          </li>
        </ul>

      <% end %>


      <!-- Navigation -->
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="#sidebarDashboards" data-bs-toggle="collapse" role="button" aria-expanded="true" aria-controls="sidebarDashboards">
            <i class="fe fe-home"></i> Dashboards
          </a>
          <div class="collapse show" id="sidebarDashboards">
            <ul class="nav nav-sm flex-column">
              <li class="nav-item">
                <a href="#" class="nav-link active">
                  Default
                </a>
              </li>
            </ul>
          </div>
        </li>
      </ul>

      <!-- Divider -->
      <hr class="navbar-divider my-3">

      <!-- Heading -->
      <h6 class="navbar-heading">
        Documentation
      </h6>

      <!-- Navigation -->
      <ul class="navbar-nav mb-md-4">
      </ul>


      <!-- Push content down -->
      <div class="mt-auto"></div>
    </div>
  </div>
</nav>

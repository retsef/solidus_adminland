<%#
# String Form Partial

This partial renders an input element for a string attribute.
By default, the input is a text field.

## Local variables:

- `f`:
  A Rails form generator, used to help create the appropriate input fields.
- `field`:
  An instance of [Administrate::Field::String][1].
  A wrapper around the String pulled from the database.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Field/String
%>

<div class="form-group" data-controller="promo-code" 
    data-promo-code-pattern-value="############"
    data-promo-code-charset-value="0123456789abcdefgh">
  <%= f.label field.attribute %>
  <%= f.hint field.attribute %>
  <div class="input-group input-group-flat">
    <%= f.text_field field.attribute, 'data-promo-code-target': 'input' %>
    <span class="input-group-text">
      <a href="#" class="input-group-link" data-action="click->promo-code#generate">
        Generate
      </a>
    </span>
  </div>
  <%= f.error_message field.attribute %>
</div>
